--!strict
-- Star Altar binding settings.

return {
	PromptHoldSeconds = 1, -- how long players hold the key/button to bind
	PromptRange = 20, -- studs from the altar's PrimaryPart where the Bind prompt appears
}
