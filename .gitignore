# Built places / models (build from source with `rojo build`)
*.rbxl
*.rbxlx
*.rbxm
*.rbxmx
*.rbxl.lock
*.rbxlx.lock

# Generated by luau-lsp / rojo sourcemap
sourcemap.json

# Local Claude Code settings
.claude/settings.local.json

# OS / editor
Thumbs.db
.DS_Store

# 3D source files (shared via Drive, too large for Git)
assets/*.glb
assets/*.fbx
assets/*.fbm/
